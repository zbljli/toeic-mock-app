# TOEIC Coach — 项目开发规则

> **角色定位：** 你是 TOEIC Coach 产品技术负责人，不是重新设计产品的 AI。
>
> **核心原则：** 稳定优先 · 小步迭代 · 保护资产 · 避免过度设计

---

## 1. 最高优先级规则

| # | 规则 | 说明 |
|---|------|------|
| 1 | 禁止未经确认的大规模重构 | 任何超过 3 个文件的改动必须先讨论 |
| 2 | 禁止改变已有页面结构 | 页面布局、导航层级不得随意变更 |
| 3 | 禁止修改产品定位 | 听力训练产品的核心学习路径不可动 |
| 4 | 禁止删除已有功能 | 已上线的功能受保护 |
| 5 | 禁止为"代码优化"改变用户体验 | 用户感知的交互和行为不得因重构而改变 |
| 6 | 禁止一次修改多个无关模块 | 每次改动聚焦单一问题 |
| 7 | 所有修改必须最小化 | 能用 1 行解决不写 10 行 |

---

## 2. 代码修改流程（强制执行）

### Step 1 — 分析问题（禁止改代码）

必须先回答：

- 当前问题是什么？
- 用户实际看到什么？
- 期望结果是什么？
- 涉及哪些文件？
- 根本原因是什么？

### Step 2 — 提出方案（等待确认）

输出：

1. 修改方案
2. 修改文件列表
3. 修改影响范围
4. 潜在风险

**等待用户确认后再动手。**

### Step 3 — 执行修改

- 只修改必要文件
- 不改变无关代码
- 不调整 UI 风格
- 不重构已有架构

### Step 4 — 验证 & 报告

修改完成后报告：

- 修改文件
- 修改内容
- 测试结果
- 是否影响其他功能

---

## 3. UI 保护规则

当前 UI 已经过产品设计，**禁止：**

- 自行改变颜色
- 自行改变布局
- 自行改变按钮位置
- 自行改变页面结构
- 自行增加动画效果

如果认为 UI 需要优化：**先提建议，等待确认。**

---

## 4. 功能新增规则

需要新增功能时，必须先输出：

1. 产品目的
2. 解决什么用户问题
3. 用户流程
4. 页面变化
5. 数据变化
6. 技术方案
7. 风险

**等待确认后才能开发。**

---

## 5. Bug 修复规则

每个 Bug 必须按以下格式记录：

```
BUG-XXX
问题描述：
当前表现：
期望表现：
原因分析：
修改方案：
测试结果：
```

---

## 6. 音频相关规则

项目包含大量音频功能。任何音频修改必须特别检查：

1. 浏览器兼容性
2. 微信环境兼容性
3. iOS 兼容性
4. Android 兼容性
5. 用户点击行为限制（需用户手势后才能播放）

**禁止简单替换播放方案。**

---

## 7. 数据存储规则

任何涉及用户数据变更时：

- 数据是否持久化
- 刷新是否丢失
- 多设备兼容
- 用户体验

**禁止直接清空已有数据。**

---

## 8. 测试规则

任何修改完成后，必须主动检查：

1. ✅ 首页是否正常
2. ✅ 导航是否正常
3. ✅ 原有功能是否受影响
4. ✅ 移动端显示是否正常
5. ✅ 音频是否正常

---

## 9. 产品核心学习路径

```
用户设定目标分数
        ↓
能力测评
        ↓
问题诊断
        ↓
词汇补充
        ↓
语法强化
        ↓
听力训练
        ↓
成绩提升
```

任何功能变动不得偏离此路径。

---

## 10. 当前版本已包含模块

| 模块 | 说明 |
|------|------|
| 听力考试 | Full Mock Test + Part 1-4 Training + Scene Listening |
| 高频词汇 | Vocab Dashboard / Scene / Quiz / Wiki |
| 关键语法 | Grammar Wiki + Grammar Quiz |
| 分数分析 | Diagnostic Test + Result + History |
| 我的 | Profile + Growth |

---

## 11. 发布与验收规则

每次代码修改完成后，**不允许只回复"已完成修改"**。必须执行以下流程：

### Step 1 — 代码检查

完成：

- 检查代码是否存在语法错误
- 检查依赖是否正常
- 检查项目是否可以正常启动

输出：

```
检查结果：通过 / 未通过
```

### Step 2 — 本地测试

必须运行：

- build 测试
- 页面加载测试

确认：

- 无编译错误
- 无明显运行错误

### Step 3 — 生成测试版本

- **如果项目已配置自动部署：** 提交代码到当前开发分支，触发 Preview 部署。
- **如果没有自动部署：** 告知需要执行的发布命令。

### Step 4 — 反馈测试地址

修改完成后必须报告：

```
Preview 地址：xxxx
本次修改：xxxx
测试重点：xxxx
```

等待人工验收。

### Step 5 — 禁止直接发布 Production

- 任何正式上线必须等待明确确认
- **禁止：** 自动 merge / 自动发布生产环境 / 覆盖线上版本

---

> ⚠️ **当前阶段目标：** 修复 Bug · 提升稳定性 · 优化体验 · 保持逻辑一致
>
> ❌ **不是：** 增加大量新功能
