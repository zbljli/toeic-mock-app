[build]
  base = "."
  command = "npx expo export --platform web && node scripts/copy-data.cjs && node scripts/patch-html.cjs"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
